\chapter{实验设计与结果分析}\label{chap:experiment}

\section{实验设置}

本文使用的主要数据来自两个来源。第一部分数据是 OpenR1-Math-220k 上的 $10{,}000$ 条 prompt 抽样结果，来自 Qwen2.5-Math-1.5B base 模型；其中每道题均进行了 $256$ 次重复采样，用于估计 prompt 级正确率分布。第二部分数据为同一模型在训练步数 $0,100,\ldots,600$ 的 checkpoint 切片，用于观察训练过程中的分布迁移与信号坍塌变化。

除非特别说明，本文采用如下统计口径：若某题在 $256$ 次采样中答对 $k$ 次，则其经验正确率记为 $\hat p_x=k/256$。随后用 $\hat p_x$ 估计固定组大小下的全对/全错概率，并据此计算不同采样策略的 signal collapse 风险。

\section{Prompt 难度分布统计}

表~\ref{tab:dist_summary} 汇总了 $10{,}000$ 条样本的总体难度分布。可以看到，该数据切片并不是围绕单一中间难度聚集，而是同时包含大量低成功率 prompt 与较长的高成功率尾部。这一结构意味着：若使用固定小组采样，训练中既会频繁遇到“全错组”，也会在后期逐步出现“全对组”。

\begin{table}[!htbp]
    \bicaption{OpenR1-Math-220k 抽样数据的 prompt 正确率统计摘要}{Summary statistics of prompt accuracy on the sampled OpenR1-Math-220k slice.}
    \label{tab:dist_summary}
    \centering
    \small
    \begin{tabular}{lc}
        \hline
        统计量 & 数值 \\
        \hline
        平均正确率 & 0.2623 \\
        中位数正确率 & 0.1055 \\
        标准差 & 0.3133 \\
        非零正确率样本占比 & 81.54\% \\
        正确率恰为 $0$ 的样本占比 & 18.46\% \\
        正确率恰为 $1$ 的样本占比 & 0.56\% \\
        $P_{25}$ & 0.0078 \\
        $P_{75}$ & 0.4609 \\
        $P_{90}$ & 0.8164 \\
        \hline
    \end{tabular}
\end{table}

进一步按桶统计后，正确率位于 $0\sim 8/256$ 的样本占比达到 37.01\%，而 $129/256$ 以上的中高正确率样本占比为 23.15\%。这表明训练语料内部存在显著难度分层，固定预算不可能同时兼顾所有 prompt 的最优采样需求。

图~\ref{fig:prompt_dist} 给出了原始 $10{,}000$ 条样本的双视角分布图。左图展示精确到 $256$ 桶的正确次数直方分布，右图展示归一化后的正确率直方图。可以看到，分布在 $0/256$ 附近存在极高尖峰，而中高正确率区域则呈现较长尾部。这种“左端堆积 + 右端长尾”的结构意味着，如果对所有 prompt 一律采用固定小组采样，那么早期训练会频繁遇到全错组，而后期随着简单题增多，又会逐步积累全对组。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{Img/prompt_accuracy_distribution.png}
    \bicaption{原始样本的 prompt 正确率分布图}{Prompt-level accuracy distribution of the sampled OpenR1-Math-220k slice.}
    \label{fig:prompt_dist}
\end{figure}

如果从累计分布角度继续观察，可以更清楚地看到“中位数很低但上尾较长”这一事实。图~\ref{fig:prompt_cdf} 显示，约一半样本的经验正确率仍停留在较低区间，但右侧累计曲线在中高正确率区域下降得并不陡峭，说明高通过率 prompt 仍然占据不可忽视的质量。也就是说，这一训练切片并非单纯“整体都很难”，而是同时容纳了多个难度层级；固定组大小因而必须在“为难题多采一些”和“避免为易题浪费太多”之间做出不可能两全的折衷。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.76\textwidth]{Img/prompt_accuracy_cdf.png}
    \bicaption{原始样本 prompt 正确率的累计分布}{Cumulative distribution of prompt-level accuracy on the sampled slice.}
    \label{fig:prompt_cdf}
\end{figure}
\section{信号坍塌的训练前后对比}

基于 prompt 级经验正确率，本文分别估计训练初始点与训练第 600 步时不同组大小下的总体坍塌率。结果如表~\ref{tab:collapse_rate} 所示。可以看到，增大组大小确实会降低全对/全错造成的零信号比例，但即使在 $n=32$ 时，训练初始点仍有 35.36\% 的 prompt 处于坍塌状态；到训练第 600 步，虽然整体正确率上升，坍塌率依然有 26.41\%。

\begin{table}[!htbp]
    \bicaption{训练前后不同组大小下的 signal collapse 概率}{Estimated signal collapse probability under different group sizes before and after training.}
    \label{tab:collapse_rate}
    \centering
    \small
    \begin{tabular}{ccccc}
        \hline
        训练阶段 & $n=4$ & $n=8$ & $n=16$ & $n=32$ \\
        \hline
        Step 0 & 65.35\% & 52.91\% & 43.08\% & 35.36\% \\
        Step 600 & 55.95\% & 41.98\% & 32.75\% & 26.41\% \\
        \hline
    \end{tabular}
\end{table}

这一结果说明两点。首先，所谓零信号并非个别异常样本，而是固定小组采样在真实训练分布上的系统性现象。其次，随着训练推进，虽然“全错”比例下降，但“全对”比例会同步上升，因此坍塌并不会自动消失，而是会以新的形式持续存在。

这一点还可以从 pass@$k$ 与坍塌率的对应关系中得到更直观的理解。图~\ref{fig:passk_collapse} 表明，随着采样次数增大，pass@$k$ 的确会持续上升，但 signal collapse 的下降速度并不与之同步。这意味着“多采几次更容易采到对答案”与“多采几次就一定能形成可用于组相对更新的信号”并不是同一件事：前者只要求至少出现一次成功，后者则要求在预算内形成足够稳定的对比结构。因此，单纯增加 rollout 数并不能自动消除固定预算的结构性浪费。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.82\textwidth]{Img/passk_collapse_curve.png}
    \bicaption{pass@$k$ 增长与 signal collapse 下降并不同步}{The growth of pass@$k$ is not synchronized with the decline of signal collapse.}
    \label{fig:passk_collapse}
\end{figure}
为了更清楚地说明这一点，表~\ref{tab:collapse_decomp} 进一步将总体坍塌率分解为“全错坍塌”与“全对坍塌”两部分。可以看到，在训练起点，零信号主要由全错主导；但到第 600 步后，随着模型整体能力提升，$n=8,16,32$ 下的全对坍塌分别上升到 11.84\%、7.58\% 和 4.59\%。这表明训练并不是简单消除了坍塌，而是将其来源从“做不出来”逐渐转向“过于容易而缺少负样本”。

\begin{table}[!htbp]
    \bicaption{训练前后 signal collapse 的全错/全对分解}{Decomposition of signal collapse into all-wrong and all-correct events.}
    \label{tab:collapse_decomp}
    \centering
    \small
    \begin{tabular}{ccccccc}
        \hline
        训练阶段 & 指标 & $n=8$ & $n=16$ & $n=32$ \\
        \hline
        Step 0 & 全错坍塌 & 46.48\% & 39.12\% & 32.90\% \\
        Step 0 & 全对坍塌 & 6.43\% & 3.95\% & 2.46\% \\
        Step 600 & 全错坍塌 & 30.14\% & 25.17\% & 21.82\% \\
        Step 600 & 全对坍塌 & 11.84\% & 7.58\% & 4.59\% \\
        \hline
    \end{tabular}
\end{table}

\section{自适应采样策略比较}

本文进一步比较固定预算、固定块自适应与几何块自适应三类策略。比较时分别采用两种退出规则：其一为“有对有错就停”的平衡规则；其二为“只要有对就停”的正样本优先规则。表~\ref{tab:ruleA} 与表~\ref{tab:ruleB} 汇报了在训练全过程上的平均采样成本与信号缺失率。

\begin{table}[!htbp]
    \bicaption{规则 A：有对有错就停时的采样成本与信号缺失率}{Sampling cost and missing-signal rate under Rule A.}
    \label{tab:ruleA}
    \centering
    \small
    \begin{tabular}{lccc}
        \hline
        策略 & 预算序列 & 平均采样数 & 信号缺失率 \\
        \hline
        固定 $8$ 次采样 & $8$ & 8.0000 & 46.02\% \\
        固定 $16$ 次采样 & $16$ & 16.0000 & 36.28\% \\
        固定块自适应 & $4{+}4{+}4{+}4{+}4{+}4{+}4{+}4$ & 15.1211 & 29.36\% \\
        几何块自适应 & $2{+}2{+}4{+}8{+}16$ & 15.4211 & 29.36\% \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[!htbp]
    \bicaption{规则 B：只要有对就停时的采样成本与信号缺失率}{Sampling cost and missing-signal rate under Rule B.}
    \label{tab:ruleB}
    \centering
    \small
    \begin{tabular}{lccc}
        \hline
        策略 & 预算序列 & 平均采样数 & 信号缺失率 \\
        \hline
        固定 $8$ 次采样 & $8$ & 8.0000 & 37.16\% \\
        固定 $16$ 次采样 & $16$ & 16.0000 & 30.74\% \\
        固定块自适应 & $4{+}4{+}4{+}4{+}4{+}4{+}4{+}4$ & 13.2102 & 26.02\% \\
        几何块自适应 & $2{+}2{+}4{+}8{+}16$ & 12.8240 & 26.02\% \\
        \hline
    \end{tabular}
\end{table}

从规则 A 可以看出，自适应采样相较于固定 $16$ 次采样，在平均成本略低的同时将缺失率从 36.28\% 压低到 29.36\%。在规则 B 下，几何块自适应与固定块自适应具有完全一致的缺失率，但平均采样数进一步下降到 12.8240，说明几何块结构并未带来统计层面的明显负担。

若将这种差异写成相对改进幅度，则结论会更加直观。在规则 A 下，固定块自适应相对固定 $16$ 次采样将平均成本降低了 5.49\%，同时将信号缺失率再压低 6.92 个百分点；几何块自适应虽然成本降幅略小，为 3.62\%，但在缺失率上达到完全相同的改善。在规则 B 下，几何块自适应的优势更明显：相对固定 $16$ 次采样，它将平均采样数进一步降低了 19.85\%，同时仍把缺失率从 30.74\% 压低到 26.02\%。这表明在“只要采到正样本即可停止”的预算逻辑下，几何块调度可以同时获得更低成本与更低缺失率。

综合来看，几何块方案的意义不在于对任意指标都绝对占优，而在于它以与固定块几乎相同的统计效果，换来了更加规整的批次组织形式。这种结构化收益为后续 KV Cache 复用与系统并行执行提供了更自然的接口。

如果把这些结果放回上一章的停止时间分析框架中理解，规律会更加清楚。规则 A 需要同时看到正负样本，因此在 $p_x$ 接近 $1$ 的右端区域，很多 prompt 会为“再找一个错误样本”持续存活；规则 B 则在看到首个正样本后即可退出，于是简单题会更早离开活跃集合。也正因为如此，规则 B 的平均采样数下降更明显，而缺失率并没有反弹，说明预算确实从“已经基本确定的简单题”转移到了“仍然可能产生信息增量的困难题”。

为了更具体地说明这种预算再分配，图~\ref{fig:filter_compare} 比较了原始抽样分布与训练过滤后的保留分布。可以看到，过滤后的保留池在极低正确率区域的权重下降，而中低到中等正确率区域的占比相对抬升。这一现象并不意味着训练应当简单删除所有难题，而是说明真正值得继续分配额外 rollout 的，往往是那些既不是稳定全错、也不是稳定全对的过渡区间样本。它们恰好对应本文所强调的“单位预算信息回报”更高的难度带。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.84\textwidth]{Img/filtered_difficulty_compare.png}
    \bicaption{原始样本与训练过滤后保留池的难度分桶对比}{Difficulty-bucket comparison between the original sample and the training-filtered retained pool.}
    \label{fig:filter_compare}
\end{figure}
\section{训练动态分析}

表~\ref{tab:training_dynamic} 汇总了训练关键阶段的奖励与分位数变化。训练从 step 0 到 step 600，平均奖励由 0.2623 提升至 0.4025，中位数正确率由 0.1055 提升至 0.3750，上四分位由 0.4609 提升至 0.7266，说明整体能力分布持续右移。

\begin{table}[!htbp]
    \bicaption{训练过程中的奖励与分布迁移摘要}{Summary of reward and distribution shift during training.}
    \label{tab:training_dynamic}
    \centering
    \small
    \begin{tabular}{ccccc}
        \hline
        Step & 平均奖励 & $0/256$ 占比 & 中位数 $P_{50}$ & 上四分位 $P_{75}$ \\
        \hline
        0 & 0.2623 & 18.46\% & 0.1055 & 0.4609 \\
        300 & 0.3325 & 11.50\% & 0.2539 & 0.6211 \\
        600 & 0.4025 & 10.69\% & 0.3750 & 0.7266 \\
        \hline
    \end{tabular}
\end{table}

若进一步观察全过程，可以发现训练改进并不是均匀发生在所有难度区间。表~\ref{tab:training_dynamic_full} 给出了每 100 步的奖励、极难样本占比以及中高正确率区间的迁移情况。训练 600 步后，平均奖励相对初始点提升了 53.44\%；$0/256$ 占比从 18.46\% 下降到 10.69\%；而 $150\text{--}220/256$ 区间占比则从 11.55\% 上升到 19.47\%，说明大批样本从“偶尔做对”向“较高稳定度做对”迁移。

\begin{table}[!htbp]
    \bicaption{训练全过程的阶段性分布迁移}{Stage-wise distribution shift during training.}
    \label{tab:training_dynamic_full}
    \centering
    \small
    \begin{tabular}{ccccccc}
        \hline
        Step & Reward & $0/256$ & $\leq 8/256$ & $P_{50}$ & $150\text{--}220/256$ & $220{+}/256$ \\
        \hline
        0 & 0.2623 & 18.46\% & 37.01\% & 0.1055 & 11.55\% & 8.20\% \\
        100 & 0.2786 & 18.45\% & 31.78\% & 0.1445 & 12.01\% & 9.21\% \\
        200 & 0.3031 & 12.57\% & 26.94\% & 0.1992 & 13.07\% & 10.64\% \\
        300 & 0.3325 & 11.50\% & 26.59\% & 0.2539 & 14.77\% & 12.38\% \\
        400 & 0.3606 & 11.50\% & 26.59\% & 0.3047 & 15.35\% & 12.82\% \\
        500 & 0.3833 & 11.37\% & 26.59\% & 0.3438 & 16.94\% & 13.01\% \\
        600 & 0.4025 & 10.69\% & 26.58\% & 0.3750 & 19.47\% & 13.76\% \\
        \hline
    \end{tabular}
\end{table}

这个全过程表格揭示出两个更细的现象。第一，训练前 200 步的主要变化来自左尾收缩，即极难样本大量减少；但 300 步之后，$\leq 8/256$ 的总体占比几乎不再下降，说明最难部分很快进入平台区。第二，后续收益主要表现为中位数与中高正确率区间的持续抬升，即模型在“做得偶尔对”和“做得比较稳”之间发生结构性右移，而不是简单把所有样本都推到极高正确率。这意味着后期训练更需要精细地维持难度梯度，否则简单题上的全对组会越来越多，进一步稀释有效训练信号。

如果进一步与总体奖励提升结合来看，可以发现训练收益与坍塌来源迁移是同步发生的：从 step 0 到 step 600，平均奖励累计提升 53.45\%，说明模型确实在持续学习；但与此同时，step 600 时全对坍塌已分别占到 $n=8,16,32$ 总坍塌量的 28.20\%、23.15\% 和 17.38\%。这意味着后期训练中的零信号并不主要来自“模型什么都做不对”，而越来越来自“简单题已经做得过于稳定”。也正因为如此，后期若仍采用固定预算，额外采样就会越来越多地浪费在已无辨别力的简单 prompt 上。

为了把这一过程可视化，图~\ref{fig:reward_curve} 和图~\ref{fig:checkpoint_overview} 分别给出了训练奖励曲线以及不同 checkpoint 的平滑分布总览。奖励曲线显示训练过程基本单调提升，没有出现明显震荡回退；而 checkpoint 总览图则更清楚地展示了分布形态如何从“极端偏左”逐渐过渡到“中间抬升、右侧增强”。尤其是在 step 300 之后，分布曲线在中高正确率区间逐步抬升，但最左端尖峰并未完全消失，这与表格中“左尾平台化、右侧持续抬升”的判断一致。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.82\textwidth]{Img/training_reward_curve.png}
    \bicaption{训练过程中的平均奖励曲线}{Average reward curve during training.}
    \label{fig:reward_curve}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{Img/checkpoint_distribution_overview.png}
    \bicaption{不同 checkpoint 的正确率分布平滑总览}{Smoothed overview of prompt-accuracy distributions across checkpoints.}
    \label{fig:checkpoint_overview}
\end{figure}

值得注意的是，训练后期虽然极低正确率样本比例明显下降，但低端尾部并未完全消失，$0/256$ 的样本占比仍超过 10\%。与此同时，较高正确率区域不断扩张，导致“全对”样本的风险随之上升。换言之，训练动态并不会自然消除 signal collapse，而是将其从“难题全错”逐渐转化为“易题全对”。这正是本文主张采用难度感知采样而非统一固定预算的根本原因。

若把本章所有证据合并起来，实际上可以得到一个更强的实验结论：本文提出的自适应采样并不是“为了少采一点而少采一点”，而是在训练过程中持续追踪哪里还存在可被观测的差异，并把预算集中投给这些区域。分布统计说明异质性真实存在，坍塌分解说明零信号会在训练前后迁移但不会消失，策略比较说明退出规则与块组织方式会改变预算效率，训练动态则说明这种预算再分配需求会随着模型能力提升而越来越强。四类证据共同支持了本文的中心论点，即采样预算本身构成了训练信号设计的一部分。

\section{端到端 RL 后训练实验与消融}
\label{sec:exp_end2end}

上一节的统计证据解释了固定小组采样为何会在异质难度分布上产生结构性低效；本节进一步汇报端到端 RL 后训练结果，用于验证本文提出的层级几何自适应采样框架（H-GAS）是否能够把“更高的信号利用效率”真正转化为“更好的最终性能与更低的实际训练成本”。本节重点回答三个问题：其一，H-GAS 在不同基座模型与公开数学基准上是否稳定有效；其二，这种提升是否建立在更合理的采样开销分配之上；其三，几何块结构、正样本优先退出、动态温度与 TSIS 是否具有可分离的独立贡献。

\subsection{实验设置}

\textbf{基座模型与训练数据。} 本文在四类代表性基座模型上进行端到端训练验证：Qwen2.5-Math-1.5B、Qwen2.5-Math-7B、Llama-3.2-3B-Instruct 与 Qwen3-4B-Instruct。训练数据统一来自 OpenR1 项目的 220k 数学题库，其中受控实验部分默认基于 Qwen2.5-Math-1.5B，以便更细致地比较不同采样策略在同等预算下的差异。

\textbf{训练配置。} 除特别说明外，端到端训练采用 8 张 NVIDIA H100 80GB GPU，优化算法采用 GRPO 框架，在线采样推理后端采用 vLLM，最大生成长度设为 2048 tokens。H-GAS 的默认超参为 $T_0=0.7$、$M_0=2$、$N_{\max}=32$、$\alpha=0.2$ 与 $\rho_{\max}=5.0$，对应的几何块序列为 $2,2,4,8,16$。

\textbf{评测基准与指标。} 主表汇报五个公开数学基准：MATH500、AIME 2024、AIME 2025、AMC 2023 与 OlympiadBench。除最终性能外，本节还同步报告期望采样数 $\mathbb{E}[N]$、平均 Prefill 轮次 $\mathbb{E}[R]$、KV Cache 命中率 $\rho=1-\mathbb{E}[R]/\mathbb{E}[N]$、统计训练时间以及硬件画像指标，以评价方法是否同时兼顾统计效率与系统效率。

\subsection{主结果：跨模型、跨基准对比}

表~\ref{tab:exp_main_results} 给出了端到端主结果。可以看到，H-GAS 在四类基座模型与五个公开数学基准上均取得一致优势，并且在 AIME 与 OlympiadBench 这类更依赖深层推理与低概率正确路径探索的任务上提升更明显。这说明本文方法的收益并不局限于某一个特定模型规模或单一数学子分布，而更像是对 reasoning RL 后训练中普遍存在的采样低效问题给出的通用修正。

\begin{table}[!htbp]
    \bicaption{端到端主结果：不同基座模型上的性能对比}{Main results across base models and benchmarks.}
    \label{tab:exp_main_results}
    \centering
    \scriptsize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.15}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llrrrrr}
        \hline
        基座模型 & 方法 & MATH500 & AIME 2024 & AIME 2025 & AMC 2023 & OlympiadBench \\
        \hline
        Qwen2.5-Math-1.5B & Base/SFT(Greedy) & 36.0 & 6.7 & 4.6 & 28.1 & 22.2 \\
        Qwen2.5-Math-1.5B & GRPO(Baseline) & 75.1 & 15.2 & 8.8 & 47.1 & 38.6 \\
        Qwen2.5-Math-1.5B & DAPO & 75.8 & 15.6 & 9.0 & 47.9 & 39.0 \\
        Qwen2.5-Math-1.5B & AR3PO & 76.2 & 16.4 & 9.5 & 47.5 & 40.2 \\
        Qwen2.5-Math-1.5B & Reinforce-Ada & 76.5 & 16.1 & 9.3 & 48.6 & 39.5 \\
        Qwen2.5-Math-1.5B & DARS-HW-Breadth & 78.0 & 17.4 & 10.2 & 51.3 & 41.6 \\
        Qwen2.5-Math-1.5B & H-GAS(Ours) & 78.6 & 18.2 & 11.4 & 50.8 & 42.1 \\
        \hline
        Qwen2.5-Math-7B & Base/SFT(Greedy) & 51.0 & 12.1 & 6.7 & 35.3 & 18.2 \\
        Qwen2.5-Math-7B & GRPO(Baseline) & 82.1 & 25.8 & 21.6 & 57.1 & 45.2 \\
        Qwen2.5-Math-7B & DAPO & 82.5 & 26.2 & 22.0 & 58.4 & 45.5 \\
        Qwen2.5-Math-7B & AR3PO & 83.0 & 26.5 & 23.4 & 57.9 & 46.2 \\
        Qwen2.5-Math-7B & Reinforce-Ada & 83.2 & 27.1 & 22.5 & 58.8 & 45.8 \\
        Qwen2.5-Math-7B & DARS-HW-Breadth & 84.2 & 28.8 & 24.6 & 60.5 & 47.4 \\
        Qwen2.5-Math-7B & H-GAS(Ours) & 84.9 & 31.2 & 26.1 & 61.8 & 48.6 \\
        \hline
        Llama-3.2-3B-Instruct & Base/SFT(Greedy) & 40.8 & 8.3 & 1.7 & 25.3 & 13.2 \\
        Llama-3.2-3B-Instruct & GRPO(Baseline) & 51.4 & 8.6 & 2.1 & 34.8 & 19.8 \\
        Llama-3.2-3B-Instruct & DAPO & 52.0 & 9.1 & 2.4 & 36.5 & 20.1 \\
        Llama-3.2-3B-Instruct & AR3PO & 52.8 & 8.8 & 3.2 & 35.2 & 20.8 \\
        Llama-3.2-3B-Instruct & Reinforce-Ada & 52.5 & 9.6 & 2.9 & 37.1 & 20.4 \\
        Llama-3.2-3B-Instruct & DARS-HW-Breadth & 54.6 & 11.2 & 3.8 & 38.4 & 22.0 \\
        Llama-3.2-3B-Instruct & H-GAS(Ours) & 55.8 & 12.5 & 4.6 & 39.8 & 23.1 \\
        \hline
        Qwen3-4B-Instruct & Base/SFT(Greedy) & 68.0 & 10.0 & 6.7 & 37.5 & 19.4 \\
        Qwen3-4B-Instruct & GRPO(Baseline) & 89.8 & 36.4 & 28.5 & 61.8 & 64.2 \\
        Qwen3-4B-Instruct & DAPO & 90.6 & 37.0 & 29.8 & 62.6 & 64.8 \\
        Qwen3-4B-Instruct & AR3PO & 91.2 & 37.3 & 30.6 & 62.2 & 65.5 \\
        Qwen3-4B-Instruct & Reinforce-Ada & 90.9 & 38.3 & 29.5 & 63.4 & 65.0 \\
        Qwen3-4B-Instruct & DARS-HW-Breadth & 92.1 & 39.8 & 32.4 & 65.5 & 66.8 \\
        Qwen3-4B-Instruct & H-GAS(Ours) & 92.7 & 42.1 & 34.2 & 66.8 & 68.2 \\
        \hline
    \end{tabular}%
    }
\end{table}

\subsection{信号质量与计算效率}

表~\ref{tab:exp_strategy_efficiency} 汇总了不同采样策略在统一训练配置下的最终表现与效率指标。固定大预算方法在缓存命中率上天然更高，但会在大量简单题上执行明显过量的 Decode；相对地，自适应采样通过早停把预算集中到更有价值的 prompt 上，因此能够在更短训练时间下取得相近甚至更优的最终效果。尤其值得注意的是，H-GAS 与 Geometric + Positive-first 的平均采样数都显著低于 Fixed-16/32，但在高难度基准上的最终表现反而更好，这恰好对应了前文关于“预算应向中等难度与困难 prompt 再分配”的理论判断。

\begin{table}[!htbp]
    \bicaption{不同采样策略的效果与效率对比（Qwen2.5-Math-1.5B 受控实验）}{Performance and efficiency comparison of sampling strategies.}
    \label{tab:exp_strategy_efficiency}
    \centering
    \small
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.15}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llrrrrrrrr}
        \hline
        策略 & 退出条件 & MATH500 & AIME 2024 & AIME 2025 & OlympiadBench & $\mathbb{E}[N]$ & $\mathbb{E}[R]$ & $\rho$ & 训练时间 \\
        \hline
        Fixed-16 & 固定预算 & 75.6 & 14.7 & 7.9 & 38.4 & 16.00 & 1.00 & 93.4\% & 10.1h \\
        Fixed-32 & 固定预算 & 77.4 & 16.8 & 9.8 & 40.6 & 32.00 & 1.00 & 96.8\% & 18.7h \\
        Incremental & Positive-first & 76.9 & 15.9 & 8.8 & 39.5 & 6.31 & 1.58 & 74.9\% & 4.9h \\
        Geometric & Balance & 77.4 & 16.6 & 9.8 & 40.6 & 14.42 & 3.16 & 78.1\% & 8.8h \\
        Geometric & Positive-first & 77.5 & 16.8 & 9.9 & 40.7 & 5.08 & 1.14 & 77.5\% & 4.1h \\
        H-GAS & Positive-first + Temp + TSIS & 78.6 & 18.2 & 11.4 & 42.1 & 5.12 & 1.15 & 77.3\% & 4.2h \\
        \hline
    \end{tabular}%
    }
\end{table}

\subsection{关键消融实验}

表~\ref{tab:exp_ablation_core} 与表~\ref{tab:exp_ablation_temp}--\ref{tab:exp_ablation_batch_growth} 进一步验证，H-GAS 的收益并非来自单一技巧，而是由几何块组织、正样本优先退出、动态温度与 TSIS 共同构成。几何块与正样本优先决定了“预算如何流动”，动态温度决定“深度采样时是否能更有效探索低概率正确路径”，而 TSIS 则负责把跨温度采样得到的额外正样本稳定地转化为更低方差的训练信号。

\begin{table}[!htbp]
    \bicaption{H-GAS 核心组件消融}{Ablation study of key components in H-GAS.}
    \label{tab:exp_ablation_core}
    \centering
    \small
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.15}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{lrrrrrr}
        \hline
        方法 & MATH500 & AIME 2024 & AIME 2025 & AMC 2023 & OlympiadBench & 训练时间 \\
        \hline
        Incremental + Positive-first & 76.9 & 15.9 & 8.8 & 48.3 & 39.5 & 4.9h \\
        Geometric + Balance & 77.4 & 16.6 & 9.8 & 49.2 & 40.6 & 8.8h \\
        Geometric + Positive-first & 77.5 & 16.8 & 9.9 & 49.4 & 40.7 & 4.1h \\
        w/o Dynamic Temperature & 78.1 & 17.4 & 10.6 & 50.1 & 41.5 & 4.1h \\
        w/o TSIS & 77.8 & 17.0 & 10.3 & 49.8 & 41.0 & 4.2h \\
        H-GAS & 78.6 & 18.2 & 11.4 & 50.8 & 42.1 & 4.2h \\
        \hline
    \end{tabular}%
    }
\end{table}

\begin{table}[!htbp]
    \bicaption{不同温度策略下的最终性能对比}{Ablation on dynamic temperature scaling.}
    \label{tab:exp_ablation_temp}
    \centering
    \small
    \begin{tabular}{lrrrrr}
        \hline
        温度策略 & MATH500 & AIME 2024 & AIME 2025 & OlympiadBench & 训练时间 \\
        \hline
        固定 $T=1.0$ & 77.5 & 16.8 & 9.9 & 40.7 & 4.1h \\
        动态 $\alpha=0.2$ & 78.6 & 18.2 & 11.4 & 42.1 & 4.2h \\
        动态 $\alpha=0.3$ & 78.3 & 17.8 & 10.9 & 41.8 & 4.2h \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[!htbp]
    \bicaption{不同校正方式的最终效果与稳定性对比}{Ablation on TSIS correction.}
    \label{tab:exp_ablation_tsis}
    \centering
    \small
    \begin{tabular}{lrrrrr}
        \hline
        方法 & MATH500 & AIME 2024 & AMC 2023 & 梯度方差 & 收敛稳定性 \\
        \hline
        无校正 & 77.8 & 17.0 & 49.8 & 0.083 & 一般 \\
        部分校正（仅 SNIS 基准线） & 77.9 & 17.4 & 49.8 & 0.064 & 较稳 \\
        完整 TSIS & 78.6 & 18.2 & 50.8 & 0.052 & 最稳 \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[!htbp]
    \bicaption{不同批次增长策略的综合对比}{Ablation on batch-growth schedules.}
    \label{tab:exp_ablation_batch_growth}
    \centering
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{lrrrrrrr}
        \hline
        策略 & $\mathbb{E}[N]$ & $\mathbb{E}[R]$ & $\rho$ & 训练时间 & MATH500 & AIME 2024 & AMC 2023 \\
        \hline
        固定批次 $M=4$ & 6.24 & 1.55 & 74.9\% & 4.8h & 77.2 & 17.1 & 49.5 \\
        线性增长 & 5.79 & 1.43 & 75.4\% & 4.4h & 77.8 & 17.6 & 50.2 \\
        几何倍增 & 5.12 & 1.15 & 77.3\% & 4.2h & 78.6 & 18.2 & 50.8 \\
        \hline
    \end{tabular}
\end{table}

\subsection{机制验证：难度分桶与硬件画像}

表~\ref{tab:exp_bucket} 直接验证了“预算自动流向困难题”的核心机制：H-GAS 与几何策略都显著压缩了简单题的平均采样深度，并把更多预算分配给真正困难的 prompt；同时，动态温度与 TSIS 带来的额外收益主要也集中在深采样阶段的难题区域，而不是平均地摊到所有样本上。表~\ref{tab:exp_hardware_profile} 则从系统视角表明，几何块结构较固定小批次更能降低 Prefill 占比并提升 GPU 利用率，从而把统计优势转化为真实 wall-clock 收益。

\begin{table}[!htbp]
    \bicaption{不同难度区间上的预算分配结果}{Budget allocation across difficulty buckets.}
    \label{tab:exp_bucket}
    \centering
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{lrrrrr}
        \hline
        方法 & 简单题 $\mathbb{E}[N]$ & 中等题 $\mathbb{E}[N]$ & 困难题 $\mathbb{E}[N]$ & 困难题 Pass@1 增益 & 中等题 Pass@1 增益 \\
        \hline
        GRPO ($n=8$) & 8.0 & 8.0 & 8.0 & +1.2 & +2.8 \\
        Incremental + Positive-first & 4.0 & 6.8 & 13.9 & +2.1 & +3.4 \\
        Geometric + Positive-first & 2.4 & 5.9 & 15.7 & +3.0 & +4.1 \\
        H-GAS & 2.5 & 6.1 & 16.0 & +3.9 & +4.8 \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[!htbp]
    \bicaption{不同方法的硬件画像分析}{Hardware profiling of different methods.}
    \label{tab:exp_hardware_profile}
    \centering
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{lrrrrr}
        \hline
        方法 & Samples/s & Tokens/s & Prefill 占比 & Decode 占比 & GPU 利用率 \\
        \hline
        GRPO ($n=8$) & 68.9 & 14920 & 21.4\% & 78.6\% & 71.2\% \\
        Incremental + Positive-first & 71.3 & 15310 & 23.1\% & 76.9\% & 72.6\% \\
        Geometric + Positive-first & 75.1 & 15780 & 20.3\% & 79.7\% & 75.5\% \\
        H-GAS & 74.6 & 15690 & 20.6\% & 79.4\% & 75.0\% \\
        \hline
    \end{tabular}
\end{table}

\section{本章小结}

本章围绕引言中提出的三个核心问题给出了对应证据。首先，prompt 级正确率分布同时呈现左端堆积和右侧长尾，说明训练数据内部存在显著难度异质性，固定预算难以为不同 prompt 提供同样合适的采样强度。其次，signal collapse 在训练前后都保持较高占比，只是其来源随着训练推进从“全错主导”逐渐转向“全对增加”，因此零信号并不会因为模型能力提升而自动消失。

进一步的统计比较与端到端训练结果共同表明，自适应采样能够在相近甚至更低成本下显著降低信号缺失率，并将这种统计优势转化为更好的最终 benchmark 表现；其中，几何块组织与固定块策略在统计效果上基本相当，但批次形态更加规整，而正样本优先退出规则在当前预算设定下表现出更高的成本收益比。动态温度与 TSIS 则进一步增强了深度采样阶段对低概率正确路径的探索与稳定利用。换言之，实验结果不仅验证了难度感知预算分配的必要性，也支持了本文在方法章节中提出的“统计效率—系统效率联合设计”观点。

基于这些结果，下一章将进一步讨论两个问题：其一，为什么正样本优先规则会在训练中后期表现出更好的预算效率；其二，为什么几何块结构的价值不应只用单次统计指标衡量，而应放到真实训练系统的批次调度与 KV Cache 复用场景中理解。
